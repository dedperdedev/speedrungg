{
  "name": "speedrungg-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code Speedrungg — starter scaffolding",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "lint": "eslint . --ext .js,.ts",
    "test": "node --test 'tests/unit/**/*.test.js'"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
