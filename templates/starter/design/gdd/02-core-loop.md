# 02 — Core Loop

> The 30-second experience. What the player does over and over.
> If you can't describe it in one paragraph, you don't have a core loop yet.

## The 30-second experience

<one paragraph: what does the player do in 30 seconds of play?>

## Verbs

The 3-7 things the player can do (no more):
1.
2.
3.

## Loop steps

1. Player sees: <what trigger / state>
2. Player does: <action>
3. Game responds: <feedback within 100ms>
4. Player sees consequence: <change to game state>
5. Loop closes: <return to step 1 with new state>

## What hooks the player back into the next loop?

- Curiosity? Mastery? Reward? Story? Social? [LABEL]

## Failure modes (test these on day 1 of prototyping)

- What if the player skips step X? Does the loop break?
- What if step Y feels too long? Where's the friction budget?
- What if the player wants to skip the loop entirely (pause/exit)? Allowed?

## Cross-references

- `mda-framework` skill (M = your loop; D = what emerges; A = how it feels)
- `juice-it-or-lose-it` skill — every loop step deserves feedback
- `first-30-seconds-rule` skill — loop must be legible in 30 seconds
